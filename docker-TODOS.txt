In order to run the project:
$ cd "sample-web-CTF"
$ "docker load -i BaseDocker/base-ctf.tar"
$ "docker-compose up"

every time you want to change something in the code, you need to run only "docker-compose up"
